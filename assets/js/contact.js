const form=document.querySelector('.contact-form');
if(form&&form.dataset.enabled==='true'){
 const status=document.getElementById('form-status'),error=document.getElementById('form-error'),button=form.querySelector('button');
 form.addEventListener('submit',async event=>{event.preventDefault();if(!form.reportValidity())return;button.disabled=true;error.hidden=true;status.textContent='Sending your enquiry…';const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),15000);try{const response=await fetch(form.action,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','Accept':'application/json'},body:new URLSearchParams(new FormData(form)).toString(),signal:controller.signal});const result=await response.json().catch(()=>null);if(!response.ok||!result||result.ok!==true)throw new Error(result&&result.error||'Not accepted');status.textContent='Thank you. Your enquiry has been received.';status.focus();form.reset();}catch(e){status.textContent='';error.textContent='We could not confirm delivery. Your message is still here. Check your connection before trying again; if the request timed out, it may already have arrived.';error.hidden=false;error.focus();}finally{clearTimeout(timer);button.disabled=false;}});
}
